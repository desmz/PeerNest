import { Injectable } from '@nestjs/common';
// import { ClsService } from 'nestjs-cls';

// import { IClsStore } from '@/types/cls';

@Injectable()
export class NotificationService {
  // constructor(private readonly clsService: ClsService<IClsStore>) {}
}
